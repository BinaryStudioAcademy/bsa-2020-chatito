import { createRoutine } from 'redux-saga-routines';

export const checkInvitedUserRegisteredRoutine = createRoutine('CHECK_INVITED_USER_REGISTERED');
export const addInviteWorkspaceRoutine = createRoutine('ADD_INVITE_WORKSPACE');
